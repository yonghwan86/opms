Task 1 — chartData 미래 더미 행 ✅ 대부분 맞음, 엣지케이스 1개
좋은 점

selectedDate + 14일 기준 명확히 명시됨 (이전 피드백 반영 ✅)
isFuture: true 플래그, 실제 가격 null 처리 올바름
getActiveCeiling 재사용으로 중복 로직 없음

체크 필요한 부분
ts// 현재 코드
while (true) {
  cur.setDate(cur.getDate() + 1);
  const ds = `...`;
  if (ds > extendEnd) break;
  ...
}
```
`lastDate === extendEnd`인 경우(trendData가 이미 +14일까지 꽉 찬 경우) 루프 자체가 실행되지 않아야 하는데, 조건이 `lastDate < extendEnd`로 진입을 막고 있으니 안전해요.

단, `toLabel(ds)` 함수가 더미 행의 날짜를 X축 포맷(`"03/28"` 등)으로 올바르게 변환하는지 확인 필요예요. 실제 `trendData`의 `label` 포맷과 일치하지 않으면 ReferenceArea, ReferenceLine x값 매칭이 깨져요.

---

## Task 2 — YAxis 도메인 ⚠️ 미래 공표일 포함 위험 잔존

이전에 지적한 문제가 해소되지 않았어요.
```
시나리오: 3.27 선택, 4.10이 DB에 미리 등록된 경우
→ Task 1로 chartData.last = 4.10
→ inRangeCeilings에 4.10 공표일 포함
→ 4.10 최고가격이 YAxis 도메인에 포함됨
→ ReferenceArea, ReferenceLine도 4.10에 그려짐
ts// 안전하게 하려면 상한 추가 필요
const todayKey = new Date().toISOString().slice(0,10).replace(/-/g,'');
const inRangeCeilings = allCeilings.filter(c => {
  const d = c.effectiveDate.replace(/-/g,'');
  return d >= firstChartDate && d <= todayKey; // today 상한
});
문서에는 "자동으로 확장된다"고만 되어 있고 이 방어 로직이 없어요.

Task 3 — 커스텀 Legend ceiling skip ✅ 정확
ts.filter((entry: any) => !entry.dataKey.startsWith("ceiling_"))
깔끔하고 올바른 방식이에요. 이전에 지적한 Tooltip 노출 문제도 확인이 필요한데, 문서에 언급이 없어요.
tsx// CustomTooltip에서도 ceiling_ 항목 필터 필요한지 확인
// Tooltip payload에도 ceiling_ 항목이 뜰 수 있음

Task 4 — 라벨 잘림 방지 ⚠️ 조건 버그 가능성
tslabel={{ value: "공표일", 
  position: isSelected && lbl === lastLabel 
    ? "insideTopLeft" 
    : "top" 
}}
isSelected가 뭔지 문서에 정의가 없어요. 선택된 공표일인지 판별하는 변수인 것 같은데, 이게 없으면 조건이 항상 false가 되어 모든 라벨이 top으로 떨어져요. 정의 추가가 필요해요.
또한 Task 1로 미래 더미 행이 추가되면 lastLabel이 미래 날짜("04/10")가 되는데, 실제로 공표일이 "04/10"인 경우가 아니면 조건이 맞지 않아요. 이 경우 오른쪽 끝 공표일 라벨("03/27")이 여전히 잘릴 수 있어요.
ts// 더 안전한 조건
const isNearEnd = chartData
  .slice(-3)  // 마지막 3개 라벨 안에 있으면 insideTopLeft
  .some(row => row.label === lbl);

Task 5 — ReferenceArea ✅ 올바름, 엣지케이스 1개
방향은 정확해요. 다만 공표일이 1개인 경우:
tsascCeilings = [3.13]
areas = [{ x1: "03/13", x2: lastChartLabel, idx: 0 }]
// → 구간 1개, idx=0 → "#f3f4f6" 색 1개만 사용
// → 교대 색 패턴 없음, 의도한 동작인지 확인 필요
기능상 문제는 없지만 의도를 명시해두면 좋아요.